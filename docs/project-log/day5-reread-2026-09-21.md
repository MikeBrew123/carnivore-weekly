# Day 5 re-read (follows `day5-first-send-read-2026-09-14.md`)

**Date:** 2026-09-21 (Pacific)
**Scope:** read, then apply the pre-registered release rule from decks `2ff6defb` and `1dd367b8`.
**Data source:** same as the first read. Supabase PostgREST with the service key from `dashboard/.env` (never printed): `drip_events`, `drip_subscribers`, `drip_survey_questions`, `drip_survey_options`, `drip_survey_responses`, `calculator_sessions_v2`. Every number is measured. Rates are unique recipients by distinct `resend_id`. Read window: all CW day-5 events from 2026-09-13 through the 2026-09-20 run (last event read 2026-09-21 10:51 UTC).

---

## Verdict

**Gate PASSED. Nothing RELEASED, because nothing is built to release.**

- No defect found. The skip path fired **6 times** and behaved correctly every time.
- By the pre-registered rule, all three held changes are now clear to go.
- But none of the three exists as a finished change. There is no branch, no commit, no inactive DB row, no draft copy, and no flag to flip for any of them. Here, "release" can only mean "start building them". That needs writer agents and design calls, not a switch. So no live config, template or DB row was changed today (see "What release means" below).

---

## 1. Deliveries

| Run date | Day-5 sent | Day-5 skipped |
|---|---|---|
| 2026-09-13 | 5 | 0 |
| 2026-09-14 | 3 | 0 |
| 2026-09-15 | 2 | 1 |
| 2026-09-16 | 0 | 0 |
| 2026-09-17 | 9 | 2 |
| 2026-09-18 | 9 | 0 |
| 2026-09-19 | 2 | 2 |
| 2026-09-20 | 3 | 1 |
| **Total** | **33** | **6** |

39 distinct subscribers reached day 5. Every event carried the new subject ("How long will this take? One tap and I'll show you."). No subscriber got day 5 twice. No duplicate send of any day since 09-12. All daily-publish runs 09-15 to 09-20 succeeded.

## 2. Engagement (33 sends)

| Metric | Frozen baseline | Day 5, cumulative | 95% Wilson |
|---|---|---|---|
| Delivered | 173 | **33** (100%) | |
| Open | 48.6% | **57.6%** (19) | 40.8 to 72.8 |
| Unique click | 5.8% | **21.2%** (7) | 10.7 to 37.8 |
| Check-in click | 1.7% | **21.2%** (7) | same |
| Bounce / complaint | | 0 / 0 | |

Open is still not distinguishable from baseline. Click sits clearly above baseline. The lower bound (10.7%) is above 5.8%, but n=33 is still small. The first read's 40% has come down to 21%, as expected. Every click went to a `journey-checkin.html?day=5` one-tap link.

## 3. `goal_target` answers

| Band | Answers |
|---|---|
| Up to 15 lb | 2 |
| 15 to 30 lb | 2 |
| 30 to 50 lb | 1 |
| 50 to 80 lb | 1 |
| More than 80 lb | 0 |
| I don't have a specific number yet | **1** |

7 answers from 7 distinct subscribers, all `answered_via = one_tap`, all with a real `subscriber_id`. Each answer matches the option id in that person's click URL. One reader tapped the same band twice and got one row, so dedup works. Band 6 has now been chosen once.

## 4. Skip path (the question this re-read existed to answer)

| # | Run | skip_reason | Calculator goal | Got day 5? | Advanced? | Next email |
|---|---|---|---|---|---|---|
| 1 | 09-15 | goal_not_weight_loss | maintain | no | yes | day 6 on 09-16 |
| 2 | 09-17 | goal_not_weight_loss | gain | no | yes | day 6 on 09-18, day 7 on 09-19 |
| 3 | 09-17 | goal_not_weight_loss | gain | no | yes | day 6 on 09-18, day 7 on 09-19 |
| 4 | 09-19 | goal_not_weight_loss | gain | no | yes | day 6 on 09-20 |
| 5 | 09-19 | no_calculator_context | (no calculator row; source homepage) | no | yes | day 6 on 09-20 |
| 6 | 09-20 | goal_not_weight_loss | maintain | no | yes (current_day 5) | day 6 due on today's run |

Checks, all passed:
- **Right people.** All 5 `goal_not_weight_loss` rows have an explicit maintain or gain on their latest calculator row. The `no_calculator_context` row has zero calculator rows. All 33 people who were sent day 5 have `goal = lose`, so no maintain or gain reader got the weight-loss email.
- **No wrong-segment copy.** Skipped readers get day 6 next, as normal ("What to say when someone asks about the bread"). Day 6 does not mention day 5, goals or weight loss. No skipped reader has a `goal_target` answer.
- **No broken links.** The day-5 check-in page, the day-6 check-in page and the day-6 blog link all return HTTP 200. The skip rows carry the right tags (`drip_day=5`, `sequence`, `skip_reason`).
- Skip rate: 6 of 39 (15.4%), lower than the 26.7% projected on 09-14. Maintain/gain: 5 of 39 (12.8%).

One note, not a defect: skip row 1 unsubscribed after their day-6 email on 09-16. That is one person. It is not a pattern, and skipping them is exactly what spared them the weight-loss email.

---

## Release decision (pre-registered rule)

Rule: release all three only if no defect was found and the skip path fired at least once and behaved correctly. **Both conditions are met.**

### What "release" means mechanically, checked item by item

| Held change | What exists today | What releasing would take |
|---|---|---|
| **Day 4 change** (2026-09-12 strategy: remove CW day 4, move `hardest_part` to day 10) | Only the recommendation in `docs/archive/reports-archive/2026-09-12-drip-redesign-strategy.md`. `data/drip-emails/day-4.html` is live and unchanged since `c3f82ecc`. `hardest_part` is active on CW day 4. There is no branch, no commit, no day-10 rewrite. | Remove a day from the sequence, rewrite day 10 (Chloe) to carry the question, move the DB question. This is new copy plus a sequence change. |
| **KD day 5 copy** (bead `carnivore-weekly-kwjf`, open) | `data/drip-emails/kd/day-5.html` is still "When does keto start feeling good?". KD `goal_target` is seeded but inactive. No Sarah-written KD day 5 exists in the repo, on any branch or in the vault. KD drip is live (`KD_DRIP_ENABLED=true`). | Sarah writes KD day 5, then the KD `goal_target` question is activated. Activating it without the copy would point the keto email at a weight-loss question. That is the exact thing the bead warns against. |
| **Ninth symptom option** (bead `carnivore-weekly-ukz5`, open) | CW `symptoms_today` has 8 options. No ninth option text, no inactive row, no decision on 7/8 exclusivity. | Decide the option and the exclusivity rule first (the bead calls it a form-level decision), then Sarah writes the card, then add the option row plus the payback card on `journey-checkin.html`. |

None of these is a flag, a config row, a merge or a template swap. Each one is unwritten work. Releasing any of them today would mean writing new copy or making a schema change on my own authority, while the house rule says writers are spawned and never imitated. It would also change what the live drip sends. So the correct move under the rule is to record that the **gate cleared and the freeze is lifted**, and make no live change.

## Verification that nothing live changed

- `data/drip-emails/day-4.html`, `day-5.html` and `kd/day-5.html`: last change `c3f82ecc` (2026-09-12), no working-tree changes. `scripts/` and `public/journey-checkin.html`: clean.
- DB, as the next scheduled run will read it: CW `goal_target` day 5 active. KD `goal_target` day 5 **inactive**. CW `hardest_part` still on day 4 and active. CW `symptoms_today` still has 8 options.
- Nothing was sent, triggered or deployed. No row was inserted, updated or deleted.

## For Brew

The three changes are no longer blocked by data. They are blocked on being built. To start them, say which one first. The natural order is:
1. KD day 5 copy (Sarah, then flip one DB flag).
2. The ninth symptom option (needs your call on exclusivity).
3. The day 4 removal (largest; changes the sequence).
